import React from 'react';
import { routeStyle } from 'complex-example/utils/style';

export default () => {
  return (
    <div style={routeStyle}>
      <p style={{ margin: 0 }}>Waiting for top of page...</p>
    </div>
  );
};
